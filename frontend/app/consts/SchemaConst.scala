package consts

object SchemaConst {
  val TYPE_STRING = "string"
  val TYPE_BOOLEAN = "boolean"
  val TYPE_ARRAY_STRING = "string[]" // TODO: Can we do this more generically?
  val TYPE_ENUM = "enum"
}